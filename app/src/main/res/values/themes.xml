<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.NewSoftware" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="Theme.NewSoftware" parent="Base.Theme.NewSoftware" />

    <style name="AdTheme">
        <item name="android:background">@color/black</item>
        <item name="android:windowBackground">@color/black</item>
    </style>


    <style name="CustomRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/custom_rating_bar</item>
        <item name="android:indeterminateDrawable">@drawable/custom_rating_bar</item>
        <item name="android:minHeight">48dp</item> <!-- Adjust the height as needed -->
    </style>
</resources>